/**
 * Versioned variant AWS S3 driver types
 * 
 * This file contains types for the versioned variant driver.
 * Versioned variant types combine common versioned types with AWS S3 base types (and flex).
 * This file does NOT introduce new fields - it only combines existing types.
 * 
 * Note: This file is a placeholder for future versioned variant functionality.
 */

// Placeholder for future versioned variant types
// When versioned types are added to the common types, this will combine:
// - SharedAwsS3DriverOptions (from types-base.ts)
// - MTFlexDriverOptions (from common types)
// - MTVersionedDriverOptions (from common types, when available)
export {};

