/**
 * Storage common types
 * 
 * This file contains remaining shared types that don't fit into the other categories.
 * These are types that are common across the storage package but not specific to
 * drivers, variants, or providers.
 */

// Currently empty - reserved for future shared types that don't fit elsewhere
export {};

